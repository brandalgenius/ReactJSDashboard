import styled from '@emotion/styled'

export const WrapperInstitutionTable = styled.div`
  position: relative;
  margin-bottom: 16px;
  padding: 8px;
  border-radius: 4px;
  border: 1px solid #e4e9f0;
  box-shadow: 0 4px 10px 0 rgba(20, 19, 34, 0.03), 0 0 10px 0 rgba(20, 19, 34, 0.02);

  .bg-gray {
    background: #f7f7f7;
    padding: 8px;

    span.label {
      color: #9a94df;
      display: inline-block;
      width: 100%;
    }
  }
`
